<!DOCTYPE html>
<html lang="ja" dir="ltr">
  <head>
    <meta charset="utf-8">
    <link rel="stylesheets" href="dchoruses.scss" />
    <title>dictations</title>
  </head>
  <body class="dclist">
    <h1 class="Partscore">Dictations</h1>
    <div class="ptitle">
      <p>
        artist:
      </p>
      <h3 class="artist"><%= @dchorus.artist %></h3>
      <h4 class="song"><%= @dchorus.songs %></h4>
    </div>
    <div class="pchart">
      <div class="detail">
        <p>
          詳細
        </p>
      </div>
        <table class="pchart">

          <tr>
            <th class="first">
              貸出者
            </th>
            <td>
              <%= @dchorus.user.username %>
            </td>
          </tr>
          <tr>
            <th>
              貸出者からの説明
            </th>
            <td>
              <%= @dchorus.discription %>
            </td>
          </tr>
          <tr>
            <th>
              貸出者からのお願い
            </th>
            <td>
              <%= @dchorus.request %>
            </td>
          </tr>

        </table>

    </div>
    <%= form_for(@dchorus, url:{controller:'dchoruses', action:'consent'})do |f| %>
    <%= f.hidden_field :id, value: @dchorus.id %>
    <% end %>

    <% if @dchorus.dcrequests.last == nil || @dchorus.dcrequests.last.rent %>
      <%= link_to "借りる", dchorus_consent_path(@dchorus) ,:class => "back_btn" %>
    <% else %>
      <p class="back_btn">貸出中</p>
      <% if @dchorus.user.id == current_user.id %>
        <%= link_to "返却", dcrequests_update_path(id: @dchorus.id), :method => :put ,:class => "back_btn" %>
      <% end %>
    <% end %>
  </body>
</html>
